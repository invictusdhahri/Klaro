-- Local-only seed data. Run via `supabase db reset` (NOT in production).
-- Auth users are created via the Auth admin API; here we only seed
-- demo rows that don't require a real user.

-- Example: synthetic anomaly flag types reference list (not a table — just a comment).
-- Add other lookup data here as needed.
